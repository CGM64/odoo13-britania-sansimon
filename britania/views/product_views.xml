<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Agregando filtro por vehiculo al modelo de product.template -->
    <record id="product_template_view_search_stock_positivo" model="ir.ui.view">
        <field name="name">product.template.search.vehicle</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='services']" position="before">
                <filter string="Stock positivo" name="stock_positivo" domain="[('qty_available', '&gt;', 0.0)]"/>
            </xpath>
        </field>
    </record>

    <record id="product_product_costo_total_tree" model="ir.ui.view">
        <field name="name">product.product.costo.total.tree</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_product_tree_view"/>
        <field name="arch" type="xml">
            <field name="name" position="before">
                <field name="chasis" optional="show" readonly="1"/>
            </field>
            <field name="uom_id" position="replace">
                <field name="costo_total" optional="show" readonly="1"/>
            </field>
            <field name="standard_price" position="before">
                <field name="uom_id" optional="show" readonly="1"/>
            </field>
        </field>
    </record>
    
</odoo>
